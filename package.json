{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "echo 'No frontend build needed — assets served from public/'"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
